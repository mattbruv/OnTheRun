-- Cast Lingo MovieScript sound.ls

global G

on sndPlay logic_ch, snd_list
  if logic_ch = G.sndCh1 then
  end if
  if logic_ch = G.sndCh2 then
  end if
end

on sndVolume logic_ch, snd_vol, snd_pan
end
